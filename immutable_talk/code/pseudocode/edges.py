#Rest and forms ?
def create_publication(request):
    if post
        if form is valid
            mutation
            save form
        else
            redirect to form page
    create form


def create_publication(request):
    if request_buffer.is_post request
        if form.isvalid a_form
            form.save( a_form )
        else
            page.redirect page

    page.redirect page


#a like object is that object
#we can place plumbing by making more edges
#or 
#not worry about it and leave mutation at the senter of method. 
#is up to you do decide where these edgest exsist and what constitutues a edges.  plumbing ? 
